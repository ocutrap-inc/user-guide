---
quick_id: 260616-q22
slug: add-image-cadence-user-guide-page-and-co
date: 2026-06-16
status: complete
branch: docs/image-cadence-v915
commits:
  - 825a24c docs: add image-cadence page + fix scout photo cadence
  - 2f3473c docs: correct capture-distance and detection-range values for v915
---

# Summary — 260616-q22: Image-cadence page + v915 value reconciliation

## What changed

**New page** `getting-started/app/image-cadence.md` ("When Your Trap Takes Photos")
— customer-facing reference for every photo trigger and its cadence: approach
photos (first on entry + 2-photo burst, then per ~3 in progress / 10 min stall),
presence & scout photos (~15 s), capture moment, post-capture (15 s burst for 1 min
→ every 2 h), time-lapse (6 h default), and on-demand. Notes power/queue/detection-
range gating. Added to `SUMMARY.md` under Getting Started → App, after Scouting Mode.

**Corrections to match firmware v915** (REQUIREMENTS §11.18 is the source of truth):
- `scouting-mode.md` — "every 5 seconds for the first minute" → **~15 s** (no 5-second cadence exists in firmware).
- `pre-capture-notification.md` — default capture distance 250 mm/"10 inches" → **8 in / 200 mm**; zones reconciled (detection out to ~34 in / 875 mm); example + alert text updated.
- `technical-specifications.md` — added **Active Detection Range ~34 in (875 mm)** row (distinct from the 13 ft sensor hardware range); detection-process step updated.
- `OcuTrap_Knowledge_Base_Complete.md` — capture distance `125–1000 mm (default 250 mm)` → **`150–460 mm / 6–18 in (default 200 mm / 8 in)`**; distance-sensor row `~10 in` → `~8 in` + detection range.

## Verification

- `grep` for `every 5 seconds`, `125–1000`, `default 250 mm`, `about 10 inches`, `10.0 in detected` across active docs → **clean**.
- Inch-based capture-distance docs (already correct at 6–18 in / default 8 in) left unchanged.
- `pdf-docs/` and `scripts/` (separate in-progress work) untouched.

## Status: complete

Branch `docs/image-cadence-v915`; 2 atomic doc commits. PR to follow.
