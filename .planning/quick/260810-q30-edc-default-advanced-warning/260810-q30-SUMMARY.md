# Quick task q30 — SUMMARY

**Spec:** DOC-20
**exit-impact:** ops
**Branch:** `codex/docs-edc-advanced-warning`

## Done

- Added a prominent warning to the canonical Enhanced Door Closing page:
  EDC Backoff is an advanced per-trap setting, defaults to 50 ms, and should
  not be changed when the door consistently finishes under the locking bar.
- Explained that an incorrect value can prevent EDC recovery or reverse the
  door too far, leaving it on top of the bar and effectively unlocked.
- Limited adjustment guidance to an empty trap, one 10 ms step at a time.
- Repeated the caveat in the More Settings overview, compact settings
  reference, and hand-maintained Knowledge Base source.
- Synced all root and `docs-site/content/` mirrors.
- Regenerated the 217-page Knowledge Base PDF and source-hash sidecar.

## Verification

- `npm run build` in `docs-site/`: passed, 105 static pages generated.
- `python3 scripts/build_kb_pdf.py`: passed, 217-page PDF generated.
- `python3 scripts/verify_kb_pdf.py`: passed (`1c6737afef43…`).
- Rendered and inspected PDF pages 56 and 83–87 at 144 DPI. Both advanced
  warning boxes, surrounding tables, procedures, page transitions, and
  repeated table headers are legible with no clipping or overlap.
- `git diff --check`: passed.
