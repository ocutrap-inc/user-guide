# Quick task q24 — Summary

**Date:** 2026-07-14
**Result:** Done — one-line CSS fix in `docs-site/app/globals.css`.

Added `scroll-padding-top: 80px` to the `html` rule (56px sticky header +
24px breathing room, matching the TOC observer's existing -80px top
rootMargin).

**Verified** against the production build (`next build` + `next start`,
Playwright):

- Clicking the "After Release" heading anchor on
  `/getting-started/handling-a-captured-animal` scrolls the heading to
  80px from viewport top — fully clear of the 56px header (was: hidden
  under it).
- Direct hash-on-load (`…#after-release`) lands at the same 80px offset
  and the TOC highlights the correct section.

No markdown content touched → no KB-PDF regen required.
