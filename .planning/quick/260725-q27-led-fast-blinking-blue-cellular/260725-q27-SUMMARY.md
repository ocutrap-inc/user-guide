# Quick task q27 — SUMMARY

**Spec:** DOC-14
**exit-impact:** ops
**Branch:** `docs/led-blue-fast-blink`

## Done

- `getting-started/led-guide.md`: new `led-diagnostics` entry, `blue` /
  `fast-blink`, context "idle, not after a command". Meaning = trying to join
  the cellular network and cannot, keeps retrying, stays offline in the app.
  Action = move outdoors / better coverage, wait 10 minutes, then contact
  support with the Trap ID. Links to trap-offline, connectivity-and-coverage,
  support. Wizard + on-page table both render from this entry.
- `troubleshooting/trap-offline-or-wont-connect.md`: row in the Step 3 LED
  table, plus a Step 4 warning hint stating a persistent fast blue blink is
  not field-fixable and should go to support.
- `OcuTrap_Knowledge_Base_Complete.md`: row in System Status LEDs.
- Root ↔ `docs-site/content/` mirrors byte-identical for all three pages.
- Spec entry DOC-14 in `.planning/REQUIREMENTS.md`.

## Notes

- `fast-blink` was already a supported pattern id in
  `docs-site/lib/led-diagnostics.ts` (renders as "Fast blinking"), so no
  renderer change was needed.
- Firmware check: `LEDManager` in the active `ocutrapv2-3-2` line drives blue
  only for door-open state and the scout yellow/blue alternation. It releases
  RGB control between status displays, so this pattern is the Particle system
  LED showing through, not an application state. Nothing to change in
  firmware for this doc.

## Follow-up

- KB PDF regenerates on merge to `main` via CI (`build-kb-pdf.yml`).
- Optional ClickUp Internal Documentation task for tracking
  (`spec: DOC-14`, `exit-impact: ops`).
