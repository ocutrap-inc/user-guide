# Quick task q23 — Fix stale v706 burst-cadence claim in updates.md

**Date:** 2026-07-03
**Spec:** follows PR #26 (image-cadence reconcile); firmware truth: `particle-firmware/ocutrapv2-3-2/REQUIREMENTS.md:786` (`ENTRY_BURST_INTERVAL_MS` aliases 15 000 ms — flat 15 s cadence, no 5 s burst)
**exit-impact:** ops

## Problem

`appendix-and-resources/updates.md:32` (v706 release note) claims Scouting Mode sends "a rapid burst of photos … about every 5 seconds for the first minute". This was never true — the cadence has always been a flat 15 s. PR #26's grep sweep fixed every other instance but missed this one (flagged in the 2026-07-03 v947 verification pass).

## Plan

1. Rewrite the bullet to describe the flat 15 s cadence only.
2. Regenerate the KB PDF (`scripts/build_kb_pdf.py`) to satisfy the `verify` gate.
3. PR to main.
